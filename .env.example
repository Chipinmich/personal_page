# Optional. Set this to your custom domain so canonical URLs, Open Graph tags,
# the sitemap and the RSS feed use it instead of the Vercel deployment URL.
# Copy this file to .env.local for local overrides.
NEXT_PUBLIC_SITE_URL=https://example.com
